{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:migrate": "node -e \"require('dotenv').config(); const {execSync}=require('child_process'); execSync('npx prisma migrate deploy --schema=../prisma/schema.prisma', {stdio:'inherit', env:process.env})\"",
    "db:generate": "node -e \"require('dotenv').config(); const {execSync}=require('child_process'); execSync('npx prisma generate --schema=../prisma/schema.prisma', {stdio:'inherit', env:process.env})\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.9.0",
    "africastalking": "^0.7.8",
    "axios": "^1.9.0",
    "bcrypt": "^5.1.1",
    "body-parser": "^2.2.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "express-session": "^1.18.1",
    "google-auth-library": "^10.5.0",
    "jsonwebtoken": "^9.0.2",
    "jwt-decode": "^4.0.0",
    "node-cron": "^3.0.3",
    "nodemailer": "^7.0.3",
    "passport-facebook": "^3.0.0",
    "prisma": "^6.9.0",
    "studio": "prisma studio --schema=./prisma/schema.prisma"
  }
}
