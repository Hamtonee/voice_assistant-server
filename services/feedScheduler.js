const feedScheduler = {
  start() {
    // Placeholder scheduler to avoid runtime import errors.
    console.log('ℹ️ feedScheduler.start() called (no-op)');
  },
  stop() {
    // Placeholder scheduler to avoid runtime import errors.
    console.log('ℹ️ feedScheduler.stop() called (no-op)');
  }
};

export default feedScheduler;
